RBAC: - kubicctl rbac interface to manage it
certificates: kubicctl certificates create <user>
Exit code: make sure in error case we always exit with an error code
kubicctl: use always fmt, not log
kubicctl: read config file for server, port and path to certificates if root
Replace homedir with os call
Wait after draining a node before continue, but how long for what?
Add Node: Make sure we choose the right kubernetes version, 
          control-plane 1.13.4 with kubeadm 1.14 does not seem to work, or
          we need to specify the kubernetes version?
